import styled from 'styled-components';

const TextButton = styled.button`
  cursor: pointer;
  background: none;
  line-height: inherit;
  color: #f0b322;
  border: none;

  &:hover {
    color: #f0920e;
    outline: none;
  }
`;

export default TextButton;
